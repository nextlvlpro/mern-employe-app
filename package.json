{
  "name": "mern-employee-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "MERN Stack Employee Management System academic project.",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "predev": "npm run stop:dev",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "stop:dev": "powershell -ExecutionPolicy Bypass -File scripts/stop-dev.ps1",
    "start": "npm start --prefix server",
    "test:e2e": "node tests/playwright-smoke.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "playwright": "^1.61.1"
  }
}
